# Ignore local environment files
app/.env.local

# Firebase service account (contains sensitive credentials)
**/stacking-rewards-tracker-firebase-adminsdk-*.json
staking Firebase/

# Node / build artifacts (safe to ignore if not already handled elsewhere)
node_modules
dist
app/dist
**/dist


